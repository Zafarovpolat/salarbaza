import crypto from'crypto';import{describe,it,expect}from'vitest';import{validateTelegramInitData}from'./telegramAuth';function make(t:string,d:number){const p=new URLSearchParams({auth_date:String(d),user:JSON.stringify({id:123456789})}),c=Array.from(p.entries()).sort(([a],[b])=>a.localeCompare(b)).map(([k,v])=>`${k}=${v}`).join('\n'),s=crypto.createHmac('sha256','WebAppData').update(t).digest();p.set('hash',crypto.createHmac('sha256',s).update(c).digest('hex'));return p.toString()}describe('tg auth',()=>{const t='1:x',n=1800000000;it('valid',()=>expect(validateTelegramInitData(make(t,n-1),t,{nowSeconds:n}).user.id).toBe(123456789));it('expired',()=>expect(()=>validateTelegramInitData(make(t,n-5000),t,{nowSeconds:n})).toThrow())})
