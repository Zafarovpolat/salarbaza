import{beforeEach,describe,it,expect,vi}from'vitest';beforeEach(()=>{vi.resetModules();process.env.NODE_ENV='test';process.env.ADMIN_SESSION_SECRET='test-secret';process.env.ADMIN_TELEGRAM_IDS='123456789'});describe('session',()=>it('binds admin ID',async()=>{const m=await import('./adminSession');expect(m.verifyAdminSession(m.createAdminSession('123456789'))).toBe(true);expect(m.verifyAdminSession(m.createAdminSession('2'))).toBe(false)}))
